// M4 — Admin dashboard overview
export function AdminDashboard() {
  return (
    <div className="flex min-h-screen items-center justify-center p-4">
      <p className="text-muted-foreground">ダッシュボード（M4で実装）</p>
    </div>
  )
}
